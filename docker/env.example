# docker/.env 로 복사해 필요한 값만 바꾼다.
MYSQL_PASSWORD=12345678
PORT=8080
# 동시 접속 상한(기본 3). 넘으면 로비에 "정원이 찼습니다"가 뜬다.
PLAYERS_MAX=3
# true 면 월드 만들기·지우기를 막는다(월드를 하나 만든 뒤에 켠다).
WORLDS_FIXED=false
# 기동 시 스폰 주변을 미리 올려 첫 입장을 빠르게 한다.
WARM_WORLDS=true
# 모두 나가도 월드를 내리지 않아 다시 들어올 때 빠르다.
KEEP_LOADED=true
